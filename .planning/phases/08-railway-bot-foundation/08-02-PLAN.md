---
phase: 08-railway-bot-foundation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/pipeline/bot/auth.py
  - src/pipeline/bot/status.py
  - src/pipeline/bot/handler.py
  - src/pipeline/bot/entrypoint.py
  - src/pipeline/bot/__init__.py
  - tests/test_bot_auth.py
  - tests/test_bot_handler.py
  - tests/test_bot_entrypoint.py
autonomous: true
requirements:
  - INFRA-04
  - BOT-01
  - BOT-02
  - BOT-11

user_setup:
  - service: railway
    why: "Persistent bot hosting ($5/month Hobby plan)"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "Telegram BotFather -> existing bot token (same token used for delivery)"
      - name: AUTHORIZED_USER_IDS
        source: "Comma-separated Telegram user IDs (same IDs from TELEGRAM_CHAT_IDS)"
      - name: GITHUB_PAT
        source: "GitHub Settings -> Developer Settings -> Fine-grained PAT -> Contents read/write scope for this repo"
      - name: GITHUB_OWNER
        source: "GitHub repository owner username"
      - name: GITHUB_REPO
        source: "GitHub repository name (e.g., Khabri)"
    dashboard_config:
      - task: "Create Railway project and deploy from GitHub repo"
        location: "Railway Dashboard -> New Project -> Deploy from GitHub Repo"

must_haves:
  truths:
    - "Bot loads authorized user IDs from AUTHORIZED_USER_IDS env var"
    - "Unauthorized users receive 'Unauthorized' response and commands are not processed"
    - "/help returns a formatted list of available commands"
    - "/status reads pipeline_status.json from GitHub Contents API and formats a health summary"
    - "Bot entrypoint builds Application with polling and auth-filtered command handlers"
  artifacts:
    - path: "src/pipeline/bot/auth.py"
      provides: "Authorization guard — load_authorized_users function"
      exports: ["load_authorized_users"]
    - path: "src/pipeline/bot/status.py"
      provides: "GitHub Contents API reader for pipeline status"
      exports: ["read_github_file", "fetch_pipeline_status"]
    - path: "src/pipeline/bot/handler.py"
      provides: "Command callbacks for /help, /status, unauthorized"
      exports: ["help_command", "status_command", "unauthorized_handler"]
    - path: "src/pipeline/bot/entrypoint.py"
      provides: "Bot main() with Application builder and run_polling"
      exports: ["main"]
  key_links:
    - from: "src/pipeline/bot/entrypoint.py"
      to: "src/pipeline/bot/handler.py"
      via: "CommandHandler registration"
      pattern: "CommandHandler.*help_command|status_command"
    - from: "src/pipeline/bot/entrypoint.py"
      to: "src/pipeline/bot/auth.py"
      via: "filters.User with loaded authorized IDs"
      pattern: "filters\\.User.*load_authorized_users"
    - from: "src/pipeline/bot/handler.py"
      to: "src/pipeline/bot/status.py"
      via: "status_command calls fetch_pipeline_status"
      pattern: "fetch_pipeline_status"
    - from: "src/pipeline/bot/status.py"
      to: "GitHub Contents API"
      via: "httpx.AsyncClient GET with Bearer token"
      pattern: "api\\.github\\.com.*contents"
---

<objective>
Build the bot core: authorization guard, /help and /status command handlers, GitHub Contents API status reader, and the bot entrypoint with python-telegram-bot Application builder.

Purpose: This is the functional bot. After this plan, the bot can run in polling mode, reject unauthorized users, respond to /help with a command list, and respond to /status with live pipeline health data read from the GitHub repo.

Output: auth.py, status.py, handler.py (populated), entrypoint.py, tests for auth, handlers, and entrypoint.
</objective>

<execution_context>
@/Users/meharban/.claude/get-shit-done/workflows/execute-plan.md
@/Users/meharban/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-railway-bot-foundation/08-RESEARCH.md
@.planning/phases/08-railway-bot-foundation/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — PipelineStatus schema created there -->

From src/pipeline/schemas/pipeline_status_schema.py (created in Plan 01):
```python
class PipelineStatus(BaseModel):
    last_run_utc: str = ""
    articles_fetched: int = 0
    articles_delivered: int = 0
    telegram_success: int = 0
    telegram_failures: int = 0
    email_success: int = 0
    sources_active: int = 0
    run_duration_seconds: float = 0.0
```

From python-telegram-bot v22 (installed in Plan 01):
```python
from telegram import Update
from telegram.ext import (
    ApplicationBuilder,
    CommandHandler,
    ContextTypes,
    MessageHandler,
    filters,
)
```

Key patterns from 08-RESEARCH.md:
- `filters.User(user_ids=set)` for auth guard
- `Application.run_polling(drop_pending_updates=True, allowed_updates=["message"])`
- `httpx.AsyncClient` for GitHub API calls in async handlers (NOT sync httpx)
- GitHub Contents API: `GET /repos/{owner}/{repo}/contents/{path}` with `Accept: application/vnd.github.raw+json`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Authorization guard and /help + /status command handlers</name>
  <files>
    src/pipeline/bot/auth.py,
    src/pipeline/bot/status.py,
    src/pipeline/bot/handler.py,
    src/pipeline/bot/__init__.py,
    tests/test_bot_auth.py,
    tests/test_bot_handler.py
  </files>
  <behavior>
    - load_authorized_users returns empty set when AUTHORIZED_USER_IDS is unset
    - load_authorized_users returns empty set when AUTHORIZED_USER_IDS is empty string
    - load_authorized_users parses "123,456" into {123, 456}
    - load_authorized_users strips whitespace: " 123 , 456 " -> {123, 456}
    - load_authorized_users ignores empty segments: "123,,456" -> {123, 456}
    - help_command replies with text containing "/help" and "/status"
    - status_command calls fetch_pipeline_status and replies with formatted status
    - status_command handles fetch failure gracefully (replies with error message, no crash)
    - unauthorized_handler replies with "Unauthorized" text
    - read_github_file returns raw file content from GitHub Contents API
    - read_github_file raises on non-200 responses
    - fetch_pipeline_status returns PipelineStatus parsed from GitHub API response
    - fetch_pipeline_status returns default PipelineStatus on API failure
  </behavior>
  <action>
    1. Create `src/pipeline/bot/auth.py`:
       - `load_authorized_users() -> set[int]`: Read `AUTHORIZED_USER_IDS` env var (comma-separated), parse to set of ints, return empty set if unset/empty. Follow the exact pattern from 08-RESEARCH.md Pattern 2.

    2. Create `src/pipeline/bot/status.py`:
       - `async def read_github_file(path: str, token: str, owner: str, repo: str) -> str`: Use httpx.AsyncClient with 10s timeout. GET `https://api.github.com/repos/{owner}/{repo}/contents/{path}` with `Accept: application/vnd.github.raw+json` and `Authorization: Bearer {token}`. Raise on non-200. Return response text.
       - `async def fetch_pipeline_status() -> PipelineStatus`: Read env vars GITHUB_PAT, GITHUB_OWNER, GITHUB_REPO. Call read_github_file for "data/pipeline_status.json". Parse JSON into PipelineStatus. On ANY exception (missing env vars, API error, parse error), log warning and return default PipelineStatus(). Never crash.

    3. Populate `src/pipeline/bot/handler.py` (replace placeholder):
       - `async def help_command(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None`: Reply with formatted help text listing all available commands: /help, /status, /run. Include a brief description for each.
       - `async def status_command(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None`: Call `fetch_pipeline_status()`. Format response with: last run time (or "Never" if empty), articles fetched, articles delivered, telegram/email success counts, active sources, run duration. Use plain text (not HTML) for readability.
       - `async def unauthorized_handler(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None`: Reply with "Unauthorized. Access denied."

    4. Update `src/pipeline/bot/__init__.py` to document the module: `"""Telegram bot — persistent polling process for Railway deployment."""`

    5. Write tests:
       - `tests/test_bot_auth.py`: Test load_authorized_users with monkeypatched env vars. Class-based pytest pattern.
       - `tests/test_bot_handler.py`: Test help_command, status_command, unauthorized_handler using mocked Update/context objects. Mock fetch_pipeline_status to avoid real API calls. Use `pytest.mark.asyncio` if needed (add pytest-asyncio to dev deps if not present) OR use `asyncio.run()` in tests. Prefer the simplest approach: test the response text logic directly by mocking `update.message.reply_text`.
       - For status.py: Test fetch_pipeline_status with monkeypatched env vars and respx-mocked httpx calls. Test both success and failure paths.
  </action>
  <verify>
    <automated>uv run pytest tests/test_bot_auth.py tests/test_bot_handler.py -x -q</automated>
  </verify>
  <done>Auth guard loads user IDs from env. /help returns command list. /status fetches and formats pipeline health. Unauthorized handler rejects. All with tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bot entrypoint with Application builder and polling</name>
  <files>
    src/pipeline/bot/entrypoint.py,
    tests/test_bot_entrypoint.py
  </files>
  <behavior>
    - main() raises RuntimeError when TELEGRAM_BOT_TOKEN is unset
    - main() raises RuntimeError when TELEGRAM_BOT_TOKEN is empty
    - main() creates Application with the provided token
    - main() registers help, status, and run handlers with auth filter
    - main() registers unauthorized catch-all handler in group 1
    - Application is configured with drop_pending_updates=True
  </behavior>
  <action>
    1. Create `src/pipeline/bot/entrypoint.py` following the pattern from 08-RESEARCH.md:
       - Import: `ApplicationBuilder`, `CommandHandler`, `MessageHandler`, `filters` from `telegram.ext`.
       - Import: `load_authorized_users` from `pipeline.bot.auth`.
       - Import: `help_command`, `status_command`, `unauthorized_handler` from `pipeline.bot.handler`.
       - `def main() -> None:`:
         - Read TELEGRAM_BOT_TOKEN from env. Raise RuntimeError if empty/missing.
         - Call `load_authorized_users()` to get authorized set.
         - Build Application: `ApplicationBuilder().token(token).build()`.
         - Create `auth_filter = filters.User(user_ids=authorized)` if authorized is not empty, else use `filters.ALL` (allow all if no whitelist configured — log warning).
         - Register handlers:
           - `CommandHandler("help", help_command, filters=auth_filter)`
           - `CommandHandler("status", status_command, filters=auth_filter)`
           - `CommandHandler("start", help_command, filters=auth_filter)` (alias for /help)
           - `MessageHandler(filters.COMMAND & ~auth_filter, unauthorized_handler)` in group=1
         - Note: /run command handler will be added in Plan 03.
         - Call `app.run_polling(drop_pending_updates=True, allowed_updates=["message"])`.
       - Add `if __name__ == "__main__": main()` guard.
       - Add `__main__.py` in `src/pipeline/bot/` so `python -m pipeline.bot.entrypoint` works, OR rely on the direct module path. Since railway.json uses `uv run python -m pipeline.bot.entrypoint`, ensure the module is runnable. The `if __name__` guard in entrypoint.py is sufficient since the module path targets entrypoint.py directly.

    2. Write `tests/test_bot_entrypoint.py`:
       - Test that main() raises RuntimeError when TELEGRAM_BOT_TOKEN is not set (monkeypatch env).
       - Test that main() raises RuntimeError when TELEGRAM_BOT_TOKEN is empty string.
       - Test Application construction by mocking ApplicationBuilder and verifying token is passed, handlers are registered, and run_polling is called with correct args. Use `unittest.mock.patch` to mock `telegram.ext.ApplicationBuilder`. Avoid actually starting the polling loop in tests.
       - Class-based pytest pattern.
  </action>
  <verify>
    <automated>uv run pytest tests/test_bot_entrypoint.py -x -q</automated>
  </verify>
  <done>Bot entrypoint constructs Application with auth-filtered handlers and runs polling. Token validation works. Tests verify construction and handler registration without starting real polling.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_bot_auth.py tests/test_bot_handler.py tests/test_bot_entrypoint.py -x -q` all pass
- `uv run pytest tests/ -x -q` full suite still passes
- `uv run python -c "from pipeline.bot.auth import load_authorized_users; print(load_authorized_users())"` returns set
- `uv run python -c "from pipeline.bot.handler import help_command, status_command, unauthorized_handler"` imports without error
- `uv run python -c "from pipeline.bot.entrypoint import main"` imports without error
</verification>

<success_criteria>
Bot authorization guard, /help command, /status command (reading from GitHub API), and entrypoint with Application.run_polling() are implemented and tested. An unauthorized Telegram user gets rejected. /help lists commands. /status shows pipeline health. The bot is ready to run on Railway (Plan 03 adds the /run dispatch command).
</success_criteria>

<output>
After completion, create `.planning/phases/08-railway-bot-foundation/08-02-SUMMARY.md`
</output>
