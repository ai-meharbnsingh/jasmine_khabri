---
phase: 01-project-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .python-version
  - .gitignore
  - src/pipeline/__init__.py
  - src/pipeline/fetchers/__init__.py
  - src/pipeline/fetchers/rss_fetcher.py
  - src/pipeline/analyzers/__init__.py
  - src/pipeline/analyzers/classifier.py
  - src/pipeline/deliverers/__init__.py
  - src/pipeline/deliverers/telegram_sender.py
  - src/pipeline/bot/__init__.py
  - src/pipeline/bot/handler.py
  - src/pipeline/schemas/__init__.py
  - src/pipeline/utils/__init__.py
autonomous: true
requirements:
  - INFRA-06

must_haves:
  truths:
    - "`uv sync` installs all dependencies without errors in a clean environment"
    - "`from pipeline.fetchers import rss_fetcher` resolves without ModuleNotFoundError"
    - "All runtime and dev dependencies are free/open-source (zero paid dependencies)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Build system, package metadata, runtime + dev dependencies, pytest config, ruff config"
      contains: "[build-system]"
    - path: "uv.lock"
      provides: "Reproducible dependency lockfile"
    - path: ".python-version"
      provides: "Python version pin for uv"
      contains: "3.12"
    - path: "src/pipeline/__init__.py"
      provides: "Root package with version string"
    - path: "src/pipeline/fetchers/rss_fetcher.py"
      provides: "Placeholder module for Phase 3"
    - path: "src/pipeline/analyzers/classifier.py"
      provides: "Placeholder module for Phase 5"
    - path: "src/pipeline/deliverers/telegram_sender.py"
      provides: "Placeholder module for Phase 6"
    - path: "src/pipeline/bot/handler.py"
      provides: "Placeholder module for Phase 8"
  key_links:
    - from: "pyproject.toml"
      to: "src/pipeline"
      via: "[tool.hatch.build.targets.wheel] packages declaration"
      pattern: 'packages = \["src/pipeline"\]'
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv sync generates lockfile from dependencies"
---

<objective>
Create the foundational repository layout: pyproject.toml with hatchling build backend, src-layout Python package named `pipeline`, placeholder subpackage modules for all future phases, uv dependency lockfile, Python version pin, and updated .gitignore.

Purpose: Every future phase depends on this package structure. If the src layout and build system declaration are correct, all imports work. If wrong, every future phase hits painful import errors.

Output: A `uv sync`-able project where `from pipeline.fetchers import rss_fetcher` resolves, all tool config lives in pyproject.toml, and uv.lock is committed for reproducibility.
</objective>

<execution_context>
@/Users/meharban/.claude/get-shit-done/workflows/execute-plan.md
@/Users/meharban/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-project-scaffold/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml with build system, dependencies, and tool config</name>
  <files>pyproject.toml, .python-version</files>
  <action>
Create `pyproject.toml` in the repo root with ALL of the following sections:

1. **[build-system]** — CRITICAL for package importability:
   ```toml
   [build-system]
   requires = ["hatchling"]
   build-backend = "hatchling.build"
   ```

2. **[project]** — Package metadata and runtime dependencies:
   ```toml
   [project]
   name = "khabri"
   version = "0.1.0"
   description = "Automated infrastructure and real estate news pipeline"
   requires-python = ">=3.12"
   dependencies = [
       "pydantic>=2.5",
       "PyYAML>=6.0",
   ]
   ```

3. **[tool.hatch.build.targets.wheel]** — Maps src layout to importable package:
   ```toml
   [tool.hatch.build.targets.wheel]
   packages = ["src/pipeline"]
   ```

4. **[dependency-groups]** — Dev dependencies (all free/open-source per INFRA-06):
   ```toml
   [dependency-groups]
   dev = [
       "pytest>=8.0",
       "pytest-cov>=5.0",
       "ruff>=0.9",
       "pre-commit>=3.0",
   ]
   ```

5. **[tool.pytest.ini_options]**:
   ```toml
   [tool.pytest.ini_options]
   testpaths = ["tests"]
   addopts = "-v --tb=short"
   ```

6. **[tool.ruff]** and sub-tables:
   ```toml
   [tool.ruff]
   line-length = 100
   target-version = "py312"

   [tool.ruff.lint]
   select = ["E", "F", "I", "UP"]

   [tool.ruff.format]
   quote-style = "double"
   ```

Also create `.python-version` containing exactly `3.12` (no trailing newline issues — uv reads this file).

IMPORTANT: Do NOT run `uv init` — manually create files to avoid conflicts with existing repo.
  </action>
  <verify>
    <automated>cat pyproject.toml && cat .python-version</automated>
  </verify>
  <done>pyproject.toml contains [build-system], [project], [tool.hatch.build.targets.wheel], [dependency-groups], [tool.pytest.ini_options], [tool.ruff], and .python-version contains "3.12"</done>
</task>

<task type="auto">
  <name>Task 2: Create src/pipeline package structure with placeholder modules</name>
  <files>
    src/pipeline/__init__.py
    src/pipeline/fetchers/__init__.py
    src/pipeline/fetchers/rss_fetcher.py
    src/pipeline/analyzers/__init__.py
    src/pipeline/analyzers/classifier.py
    src/pipeline/deliverers/__init__.py
    src/pipeline/deliverers/telegram_sender.py
    src/pipeline/bot/__init__.py
    src/pipeline/bot/handler.py
    src/pipeline/schemas/__init__.py
    src/pipeline/utils/__init__.py
  </files>
  <action>
Create the full src/pipeline/ directory tree. Every directory needs an `__init__.py`.

**src/pipeline/__init__.py** — Root package with version:
```python
"""Khabri: Automated infrastructure and real estate news pipeline."""

__version__ = "0.1.0"
```

**Subpackage __init__.py files** — Each should have a brief docstring:
- `src/pipeline/fetchers/__init__.py`: `"""News source fetchers (RSS, GNews API)."""`
- `src/pipeline/analyzers/__init__.py`: `"""AI analysis and classification."""`
- `src/pipeline/deliverers/__init__.py`: `"""Delivery channels (Telegram, Email)."""`
- `src/pipeline/bot/__init__.py`: `"""Telegram bot command handlers."""`
- `src/pipeline/schemas/__init__.py`: `"""Pydantic schema models for data validation."""`
- `src/pipeline/utils/__init__.py`: `"""Shared utilities and data loaders."""`

**Placeholder modules** — Each must be importable (not empty files). Use a module docstring + a placeholder comment:
- `src/pipeline/fetchers/rss_fetcher.py`: `"""RSS feed fetcher using feedparser + httpx."""`
- `src/pipeline/analyzers/classifier.py`: `"""Article priority classifier using Claude/Gemini."""`
- `src/pipeline/deliverers/telegram_sender.py`: `"""Telegram message delivery."""`
- `src/pipeline/bot/handler.py`: `"""Telegram bot command handler."""`

Each placeholder file should contain ONLY the docstring — no imports, no classes, no functions. They are pure placeholders proving the import path works.
  </action>
  <verify>
    <automated>find src/pipeline -name "*.py" | sort && python -c "import ast; [ast.parse(open(f).read()) for f in __import__('glob').glob('src/pipeline/**/*.py', recursive=True)]"</automated>
  </verify>
  <done>src/pipeline/ directory tree exists with __init__.py in every subpackage and all placeholder modules parse as valid Python</done>
</task>

<task type="auto">
  <name>Task 3: Update .gitignore, run uv sync, and verify package imports</name>
  <files>.gitignore, uv.lock</files>
  <action>
1. **Update .gitignore** — Replace current contents with a comprehensive ignore list. Key changes from existing:
   - KEEP: .env, .env.*, __pycache__/, *.py[cod], IDE files, OS files, test artifacts
   - REMOVE: `data/history.json` (INFRA-02 requires state files be committed to repo)
   - REMOVE: `data/sent_news.json` and `data/events.json` (these should not be gitignored — they are state files)
   - ADD: `*.onnx`, `*.bin`, `*.pt`, `*.safetensors` (large model files)
   - ADD: `.ruff_cache/`
   - KEEP: `screenshots/`, `traces/` (test artifacts)
   - Do NOT gitignore `data/seen.json`, `data/history.json`, `data/config.yaml`, or `data/keywords.yaml`

   The .gitignore should have these sections:
   - Environment variables (secrets)
   - Python
   - Virtual environments
   - IDE
   - OS
   - Test artifacts
   - Large model files
   - Linter cache

2. **Run `uv sync`** to install all dependencies and generate uv.lock:
   ```bash
   uv sync
   ```
   This installs the `khabri` package in editable mode (because [build-system] is declared), plus all dev dependencies.

3. **Verify the package is importable:**
   ```bash
   uv run python -c "from pipeline.fetchers import rss_fetcher; print('OK: fetchers')"
   uv run python -c "from pipeline.analyzers import classifier; print('OK: analyzers')"
   uv run python -c "from pipeline.deliverers import telegram_sender; print('OK: deliverers')"
   uv run python -c "from pipeline.bot import handler; print('OK: bot')"
   uv run python -c "from pipeline.schemas import __doc__; print('OK: schemas')"
   uv run python -c "from pipeline.utils import __doc__; print('OK: utils')"
   ```
   ALL must print OK. If any fails with ModuleNotFoundError, the [build-system] or [tool.hatch.build.targets.wheel] config is wrong — fix before proceeding.

4. **Verify uv.lock was generated:**
   ```bash
   test -f uv.lock && echo "uv.lock exists" || echo "MISSING"
   ```

5. **Verify with `uv pip show`:**
   ```bash
   uv pip show khabri
   ```
   Must show the package is installed. If it does not, the build system is not declared correctly.
  </action>
  <verify>
    <automated>uv run python -c "from pipeline.fetchers import rss_fetcher; from pipeline.analyzers import classifier; from pipeline.deliverers import telegram_sender; from pipeline.bot import handler; print('ALL IMPORTS OK')" && test -f uv.lock && echo "LOCKFILE OK"</automated>
  </verify>
  <done>uv sync succeeds, all `from pipeline.X import Y` imports resolve, uv.lock exists and is committed, .gitignore updated to not exclude state data files</done>
</task>

</tasks>

<verification>
1. `uv sync` completes without errors
2. `uv run python -c "from pipeline.fetchers import rss_fetcher"` succeeds
3. `uv.lock` exists in repo root
4. `.python-version` contains "3.12"
5. `pyproject.toml` has [build-system] with hatchling
6. All subpackages have `__init__.py`
7. `.gitignore` does NOT exclude data/seen.json or data/history.json
8. No paid dependencies in pyproject.toml
</verification>

<success_criteria>
- Running `uv sync` in the repo root installs all deps and the khabri package
- Every `from pipeline.{subpackage} import {module}` resolves without error
- uv.lock is present and committed
- All dependencies are free/open-source (INFRA-06 compliance)
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold/01-01-SUMMARY.md`
</output>
