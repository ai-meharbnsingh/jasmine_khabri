---
phase: 05-ai-analysis-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/schemas/ai_response_schema.py
  - src/pipeline/schemas/ai_cost_schema.py
  - src/pipeline/schemas/article_schema.py
  - src/pipeline/analyzers/cost_tracker.py
  - src/pipeline/utils/loader.py
  - data/ai_cost.json
  - pyproject.toml
  - tests/test_ai_response_schema.py
  - tests/test_cost_tracker.py
autonomous: true
requirements:
  - AI-07

must_haves:
  truths:
    - "AICost schema loads from and saves to data/ai_cost.json following gnews_quota.json pattern"
    - "Monthly cost budget gates enforce warning at $4.00 and degradation at $4.75"
    - "BatchClassificationResponse Pydantic model validates structured AI output with priority, summary, and entity fields"
    - "Article schema has new AI-populated fields (priority, location, project_name, budget_amount, authority)"
    - "anthropic and google-genai are installable project dependencies"
  artifacts:
    - path: "src/pipeline/schemas/ai_response_schema.py"
      provides: "BatchClassificationResponse and ArticleAnalysis Pydantic models for structured AI output"
      exports: ["ArticleAnalysis", "BatchClassificationResponse"]
    - path: "src/pipeline/schemas/ai_cost_schema.py"
      provides: "AICost Pydantic model for monthly cost tracking"
      exports: ["AICost"]
    - path: "src/pipeline/schemas/article_schema.py"
      provides: "Article model extended with AI-populated fields"
      contains: "priority"
    - path: "src/pipeline/analyzers/cost_tracker.py"
      provides: "Cost loading, saving, budget checking, cost accumulation"
      exports: ["load_ai_cost", "save_ai_cost", "check_budget", "record_cost"]
    - path: "data/ai_cost.json"
      provides: "Seed cost tracking state file"
      contains: "month"
    - path: "tests/test_ai_response_schema.py"
      provides: "Schema validation tests for AI response models"
      min_lines: 30
    - path: "tests/test_cost_tracker.py"
      provides: "Cost tracker unit tests with budget gate verification"
      min_lines: 60
  key_links:
    - from: "src/pipeline/analyzers/cost_tracker.py"
      to: "src/pipeline/schemas/ai_cost_schema.py"
      via: "AICost model import"
      pattern: "from pipeline\\.schemas\\.ai_cost_schema import AICost"
    - from: "src/pipeline/analyzers/cost_tracker.py"
      to: "data/ai_cost.json"
      via: "JSON load/save"
      pattern: "json\\.loads|write_text"
    - from: "src/pipeline/utils/loader.py"
      to: "src/pipeline/schemas/ai_cost_schema.py"
      via: "AICost model import for load/save"
      pattern: "from pipeline\\.schemas\\.ai_cost_schema import AICost"
---

<objective>
Set up all AI pipeline contracts, schemas, cost tracking infrastructure, and SDK dependencies so that Plan 05-02 can implement the classifier against stable interfaces.

Purpose: Establish the data contracts (AI response schema, cost schema, Article extensions) and cost-tracking logic before the classifier implementation. This prevents Plan 05-02 from having to define contracts and implement simultaneously.

Output: Pydantic schemas for AI response and cost tracking, cost tracker module with budget gates, Article schema extensions, seed data files, and SDK dependencies installed.
</objective>

<execution_context>
@/Users/meharban/.claude/get-shit-done/workflows/execute-plan.md
@/Users/meharban/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-analysis-pipeline/05-CONTEXT.md
@.planning/phases/05-ai-analysis-pipeline/05-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/pipeline/schemas/article_schema.py:
```python
class Article(BaseModel):
    title: str
    url: str
    source: str
    published_at: str
    summary: str = ""
    fetched_at: str
    relevance_score: int = 0
    geo_tier: int = 0
    dedup_status: Literal["NEW", "DUPLICATE", "UPDATE", ""] = ""
    dedup_ref: str = ""
```

From src/pipeline/schemas/gnews_quota_schema.py (pattern to follow for AICost):
```python
class GNewsQuota(BaseModel):
    date: str  # ISO date string "YYYY-MM-DD" in UTC
    calls_used: int = 0
    daily_limit: int = 25
```

From src/pipeline/utils/loader.py:
```python
def load_config(path: str | Path = "data/config.yaml") -> AppConfig: ...
def load_keywords(path: str | Path = "data/keywords.yaml") -> KeywordsConfig: ...
def load_seen(path: str | Path = "data/seen.json") -> SeenStore: ...
def save_seen(store: SeenStore, path: str | Path = "data/seen.json") -> None: ...
```

From data/gnews_quota.json (pattern for ai_cost.json seed):
```json
{
  "date": "1970-01-01",
  "calls_used": 0,
  "daily_limit": 25
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AI response schema, AICost schema, and Article extensions</name>
  <files>
    src/pipeline/schemas/ai_response_schema.py,
    src/pipeline/schemas/ai_cost_schema.py,
    src/pipeline/schemas/article_schema.py,
    data/ai_cost.json,
    pyproject.toml,
    tests/test_ai_response_schema.py
  </files>
  <behavior>
    - ArticleAnalysis validates index (int), priority ("HIGH"/"MEDIUM"/"LOW"), summary (str), location (str), project_name (str), budget (str), authority (str)
    - ArticleAnalysis rejects priority values outside HIGH/MEDIUM/LOW
    - BatchClassificationResponse validates articles list of ArticleAnalysis
    - BatchClassificationResponse round-trips through model_dump_json / model_validate_json
    - AICost validates month ("YYYY-MM"), total_input_tokens (int), total_output_tokens (int), total_cost_usd (float), call_count (int)
    - AICost defaults all numeric fields to 0
    - Article model accepts new optional fields: priority (Literal["HIGH","MEDIUM","LOW",""] = ""), location (str = ""), project_name (str = ""), budget_amount (str = ""), authority (str = "")
    - Existing Article construction (from Phase 3/4 callers) still works unchanged with all new fields defaulting to empty
    - anthropic and google-genai are added to pyproject.toml dependencies
  </behavior>
  <action>
    1. Run `uv add anthropic google-genai` to add SDK dependencies to pyproject.toml and install them.

    2. Create `src/pipeline/schemas/ai_response_schema.py`:
       - ArticleAnalysis(BaseModel): index (int), priority (Literal["HIGH","MEDIUM","LOW"]), summary (str), location (str = ""), project_name (str = ""), budget (str = ""), authority (str = "")
       - BatchClassificationResponse(BaseModel): articles (list[ArticleAnalysis])
       - These models are used as the `output_format` for Anthropic's `.parse()` method — they MUST be plain Pydantic models without custom validators that would break structured output.

    3. Create `src/pipeline/schemas/ai_cost_schema.py`:
       - AICost(BaseModel): month (str, format "YYYY-MM"), total_input_tokens (int = 0), total_output_tokens (int = 0), total_cost_usd (float = 0.0), call_count (int = 0)
       - Follow gnews_quota_schema.py pattern (simple Pydantic model, no complex validators).

    4. Extend `src/pipeline/schemas/article_schema.py`:
       - Add to Article: priority: Literal["HIGH", "MEDIUM", "LOW", ""] = ""
       - Add to Article: location: str = ""
       - Add to Article: project_name: str = ""
       - Add to Article: budget_amount: str = "" (named budget_amount to avoid collision with Pydantic BaseModel internals)
       - Add to Article: authority: str = ""
       - All new fields MUST have defaults so Phase 3/4 constructors remain unchanged.

    5. Create `data/ai_cost.json` seed file following gnews_quota.json pattern:
       ```json
       {
         "month": "1970-01",
         "total_input_tokens": 0,
         "total_output_tokens": 0,
         "total_cost_usd": 0.0,
         "call_count": 0
       }
       ```

    6. Create `tests/test_ai_response_schema.py` with test classes for ArticleAnalysis, BatchClassificationResponse, and AICost validation. Verify Article backward compatibility (existing Phase 3/4 constructors still work).
  </action>
  <verify>
    <automated>uv run pytest tests/test_ai_response_schema.py -x</automated>
  </verify>
  <done>
    - ArticleAnalysis and BatchClassificationResponse models validate and round-trip correctly
    - AICost model loads from seed JSON
    - Article schema extended with 5 new defaulted fields
    - Existing tests still pass: `uv run pytest tests/ -x`
    - anthropic and google-genai in pyproject.toml dependencies
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cost tracker module with budget gates</name>
  <files>
    src/pipeline/analyzers/cost_tracker.py,
    src/pipeline/utils/loader.py,
    tests/test_cost_tracker.py
  </files>
  <behavior>
    - load_ai_cost("data/ai_cost.json") returns AICost model (empty defaults if file missing)
    - load_ai_cost resets to current month if stored month differs from current month (monthly reset)
    - save_ai_cost(cost, path) writes AICost to JSON file with indent 2 and trailing newline
    - check_budget(cost) returns "ok" when total_cost_usd < $4.00
    - check_budget(cost) returns "warning" when total_cost_usd >= $4.00 and < $4.75
    - check_budget(cost) returns "exceeded" when total_cost_usd >= $4.75
    - record_cost(cost, input_tokens, output_tokens, provider="claude") returns new AICost with accumulated tokens and computed cost
    - record_cost uses Claude Haiku 4.5 pricing ($1/$5 MTok) for provider="claude"
    - record_cost uses Gemini 2.5 Flash pricing ($0.30/$2.50 MTok) for provider="gemini"
    - record_cost increments call_count by 1
    - record_cost returns new AICost (functional style, no mutation) via model_copy
    - load_ai_cost and save_ai_cost added to loader.py following save_seen pattern
  </behavior>
  <action>
    1. Create `src/pipeline/analyzers/cost_tracker.py`:
       - Constants: CLAUDE_HAIKU_INPUT = 1.0 / 1_000_000, CLAUDE_HAIKU_OUTPUT = 5.0 / 1_000_000, GEMINI_FLASH_INPUT = 0.30 / 1_000_000, GEMINI_FLASH_OUTPUT = 2.50 / 1_000_000
       - Constants: MONTHLY_BUDGET = 5.00, BUDGET_WARNING_THRESHOLD = 4.00, BUDGET_DEGRADE_THRESHOLD = 4.75
       - check_budget(cost: AICost) -> Literal["ok", "warning", "exceeded"]: threshold checks on total_cost_usd
       - record_cost(cost: AICost, input_tokens: int, output_tokens: int, provider: Literal["claude", "gemini"] = "claude") -> AICost: compute cost from token counts and provider pricing, return cost.model_copy(update={accumulated fields})

    2. Add to `src/pipeline/utils/loader.py`:
       - load_ai_cost(path: str | Path = "data/ai_cost.json") -> AICost: load JSON, validate, auto-reset if month differs from current month (like gnews_quota.json monthly reset pattern). Return empty AICost with current month if file missing.
       - save_ai_cost(cost: AICost, path: str | Path = "data/ai_cost.json") -> None: write JSON (same pattern as save_seen).

    3. Create `tests/test_cost_tracker.py`:
       - TestCheckBudget: ok/warning/exceeded thresholds
       - TestRecordCost: claude pricing, gemini pricing, accumulation, call_count increment, immutability (original not mutated)
       - TestLoadAiCost: missing file, valid file, monthly reset
       - TestSaveAiCost: round-trip (save then load)
  </action>
  <verify>
    <automated>uv run pytest tests/test_cost_tracker.py -x</automated>
  </verify>
  <done>
    - Cost tracker functions compute correct costs for Claude Haiku and Gemini Flash pricing
    - Budget check returns "ok", "warning", or "exceeded" at correct thresholds ($4.00 and $4.75)
    - Monthly reset triggers when stored month differs from current
    - Functional style: record_cost returns new AICost, never mutates input
    - Full test suite still green: `uv run pytest tests/ -x`
  </done>
</task>

</tasks>

<verification>
- All new schemas validate correctly: `uv run pytest tests/test_ai_response_schema.py -x`
- Cost tracker logic is correct: `uv run pytest tests/test_cost_tracker.py -x`
- No regressions in existing tests: `uv run pytest tests/ -v --tb=short`
- Dependencies installed: `uv run python -c "import anthropic; import google.genai; print('OK')"`
</verification>

<success_criteria>
- AI response schema (ArticleAnalysis, BatchClassificationResponse) and AICost schema exist as Pydantic models
- Article schema extended with priority, location, project_name, budget_amount, authority (all optional with defaults)
- Cost tracker module implements load/save/check/record with correct pricing constants
- Budget gates enforce $4.00 warning and $4.75 degradation thresholds
- data/ai_cost.json seed file exists
- anthropic and google-genai in pyproject.toml dependencies
- All tests pass (new + existing)
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-analysis-pipeline/05-01-SUMMARY.md`
</output>
